
public class 链表中环的入口节点 {
	public static ListNode EntryNodeloop (ListNode pHead) {
		if (pHead == null || pHead.next == null) {
			return null;
		}
		// 如果有环的话
		ListNode n1 = pHead; // 走一步的 指针
		ListNode n2 = pHead; // 走两步的 指针
		ListNode n = pHead; // 记录 他们相遇的节点
		// 快慢指针 如果有换
		while 
	}
	
	
}
class ListNode {
	int val;
	ListNode next = null;
	public ListNode(int val) {
		// TODO Auto-generated constructor stub
		this.val = val;
	}
}
